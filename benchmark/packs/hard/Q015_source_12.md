# Reference Article: Privileged Access Control Standard

A reference article about Elystrum Security's internal rules describes privileged credential rotation intervals. It states that Omega-tier credentials uses **every 21 days**.

Source type: Secondary internal summary. Consult the active governing policy if a conflict exists.
